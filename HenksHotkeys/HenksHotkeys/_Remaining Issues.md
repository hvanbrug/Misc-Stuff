# Still on the list

2. Layout via panels instead of arithmetic (coupled to #1).
Partly modernised: data tabs lay out from `rows` + `columns` + `gapBefore`/`indent`/`blank`, so the old `ShiftLineByThird/Half` cursor hacks are gone for them (they survive only in the two remaining code tabs and the geometry tests). But buttons are still placed at absolute X/Y on a `Canvas` (`DataTabModel` computes `col*ColWidth` / `rowOffset*RowHeight`). An `ItemsControl` + `UniformGrid`/`WrapPanel` + `DataTemplate` would hand layout to WPF. **Still open.**

3. Drop the static globals → DI + MVVM-lite.
**Unchanged / open.** `AppState` is still a static bag and the window news up everything. A small service container + a `MainViewModel` (inject `SettingsStore`, hotkey manager, sender) would make it testable and cleaner. Medium effort.

# Completed

1. **DONE** — Content as data, not code.
The seven text/content tabs (Symbols, Comments, Prompt Helpers, Greek, Russian, Misc, Sensitive) are now data in `%LocalAppData%\HenksHotkeys\tabs.json` (seeded from an embedded default) and rendered by `DataTabModel` — edit JSON + **Reload configuration** instead of recompiling. Emojis and Tools stay built-in code tabs, referenced from the JSON via `{"builtin": "..."}`.
*Still optional:* move the Emojis catalog (EmojisTab.cs is still ~3,400 lines) to data, and add a named-action registry so the Tools action buttons could be data too.

4. **DONE** — Async sending.
`TextSender`'s activate→send dance now `await`s `Task.Delay` instead of `Thread.Sleep`, so the message pump keeps running and the window stays responsive (draggable/scrollable) mid-send. The entry points (`SendText`/`SendInputKeys`/`GetSelectedTextThroughClipboard`) return `Task`; buttons and hotkeys fire-and-forget them. Continuations stay on the UI/STA thread (no `ConfigureAwait(false)`), so `Clipboard` access remains valid. A `SemaphoreSlim` gate serialises sends — the synchronous version got that for free by blocking the UI thread, so concurrent clicks can't interleave keystrokes now. Verified end-to-end (UI-Automation invoke → text lands once in Notepad).

5. **DONE** — Unit tests for the pure logic.
`HenksHotkeys.Tests` exists (52 tests): HotkeyParser, EmojiImageProvider.ToTwemojiStem, AppState.StripEmojis, the `{Enter}`/brace send tokenizer, and TabModel geometry — plus the newer Secrets (passphrase/DPAPI), versioning/merge (CRDT, reconcile, collapse), and layout (gap / blank / normalize) tests.

6. **DONE** — Quarantine the Win32 interop.
All Win32 interop moved to a shared **`PInvoke`** library (separate `CSharp-Tools` repo, cloned side-by-side, relative `ProjectReference`; multi-targets `net9.0;net10.0`); `Native/NativeMethods.cs` is **deleted** (the whole `HenksHotkeys.Native` namespace is gone). OS-integration bits sit behind small interfaces where they're reused (A = consolidate the calls; B = interface boundary) and behind their existing component/helper boundary otherwise.
*PInvoke review + tidy-up:* clean two-layer pattern (`Win32` wrappers + private `Native` DllImports, split by area). Fixed the bogus `WM_NULL_ = 0xFFFF`, added a `SendInput(INPUT[])` overload (was IntPtr-only, leaking marshalling), made `dwExtraInfo` `IntPtr` (was `UIntPtr`), and grew it with `SetForegroundWindow`/`IsWindow`.
*Slice 1 — hotkeys + input send:* `HotkeyParser` (MOD_* flags), `GlobalHotkeyManager` (`RegisterHotKey`/`UnregisterHotKey`/`WM_HOTKEY`), and `TextSender` (`SendInput` + INPUT structs + KEYEVENTF/VK/MOD, `IsWindow`, `SetForegroundWindow`) call `PInvoke.Win32`. First interface: **`IGlobalHotkeys`** (`GlobalHotkeyManager` implements it; `App` holds the interface).
*Slice 2 — foreground window:* **`IForegroundWindow`** (`Core/ForegroundWindow.cs`, `AppState.Foreground`) wraps `GetForegroundWindow`/`SetForegroundWindow`/`IsWindow` and the `AttachThreadInput` foreground "dance". Consumers: `HotkeyWindow.TrackActiveWindow`/`ShowUi`, `DialogChrome.ForceForeground`, `TextSender.ActivateTarget`, `TrayIcon.ShowMenu`.
*Slice 3 — window snap/fit:* **`IWindowFit`** (`Core/WindowFit.cs`, `AppState.WindowFit`) wraps `GetAncestor`/`ShowWindow`/`GetWindowRect`/`SetWindowPos`/`DwmGetWindowAttribute`(extended-frame)/work-area, plus the `ApplyBounds` composite (ENTER/EXIT size-move for Hyper-V RDP). `AppActions` (fit policy) + `ElevatedFit` migrated. **Marshalling fix:** `ApplyBounds` captures the `SetWindowPos` error before the trailing `SendMessage` (now `SetLastError=true` in the lib) can clobber it — preserving the ACCESS_DENIED→elevated-window detection.
*Slice 4 — DWM frame theming:* `Theme.ApplyDarkFrame`/`ApplyRoundedCorners` call `Win32.DwmSetWindowAttribute` (`Win32.Dwmapi.cs`). No new interface — `Theme` is already the chrome boundary.
*Slice 5 — tray icon:* `TrayIcon` calls `Win32.Shell_NotifyIcon`/`ExtractIconEx` (`Win32.Shell.cs`), `LoadIcon`/`DestroyIcon` (`Win32.User32.Icons.cs`), `NOTIFYICONDATAW` + `NIM_*`/`NIF_*`. No new interface — `TrayIcon` is the component boundary. Icon confirmed live via UI-Automation (tray overflow shows "Henk's Hotkeys").
*Slice 6 — low-level mouse-wheel hook:* `HotkeyWindow`'s wheel hook calls `Win32.SetWindowsHookEx`/`UnhookWindowsHookEx`/`CallNextHookEx` + `HookProc` + `MSLLHOOKSTRUCT` (`Win32.User32.Hooks.cs`) + `GetModuleHandle` (`Win32.Kernel.Module.cs`).
*Slice 7 — `HotkeyWindow` + delete `NativeMethods`:* migrated the window's remaining interop and **deleted `Native/Native.cs`** (last consumer). New lib pieces: **window-style helpers** (`Win32.User32.WindowStyles.cs` — `Get`/`Set`/`Has`/`Add`/`RemoveStyle` + `*ExStyle` twins over `GetWindowLongPtr`/`SetWindowLongPtr`, + `WS_*`/`WS_EX_*`/`GWL_*`) so call sites never touch `GetWindowLongPtr` by hand; **DPI** (`Win32.User32.Dpi.cs`); `GetCursorPos` + `HWND_*` sentinels + `GetWorkArea`/`GetPrimaryWorkArea` composites (`Win32.User32.Windowing.cs`) + `MINMAXINFO`; `WindowFit.WorkArea` delegates to `Win32.GetWorkArea` (DRY). Each slice was live-verified; 116 tests pass throughout. **`NativeMethods` no longer exists.**

## New items that emerged after the original list

7. **DONE** — Go fully WPF by dropping UseWindowsForms.
The tray NotifyIcon (Shell_NotifyIcon), Clipboard (System.Windows.Clipboard), and Screen/monitor (Win32) were all replaced; no dual-framework dependency remains.

8. **DONE** — Button-gap constant, starting at 2px (gap between buttons, horizontal and vertical).
`Layout.ButtonGap` (2px) feeds `ColWidth`/`RowHeight` and the multi-cell button width. The per-tab `gap` field was removed.

9. **DONE** — Edge-gap constant, starting at 2px (gap between edges/containers and any controls — window edges, the tab control, etc.).
`Layout.EdgeGap` (2px): buttons start at `(EdgeGap, EdgeGap)`, `ContentWidth`/`Height` add a trailing edge-gap, and the window border carries `EdgeGap` padding. The per-tab `originX`/`originY` fields were removed, and the dead `TabExporter` was deleted.

10. **DONE** — Calculated corner controls + calculated collapsed size (both corners).
The strip's hand-tuned margins are gone: both the left cluster (○ ▲ ☺) and the right cluster (🔄 ⌫. ⇚, ↩ ▲) sit at `Layout.EdgeGap` from the window edges via the border padding, the strip auto-sizes to its controls (no fixed `Height = 26`), and the indicator spacing uses `EdgeGap`. The collapsed window is now derived — `CollapsedSize()` measures the left cluster and adds the border + edge-gap chrome (≈ 79×30) instead of the old hardcoded 84×28.
*Note:* the right cluster's per-glyph `RaiseTop` baseline nudges and the fixed inter-icon gap remain — they're font-metric cosmetics, not edge positioning.

11. Full-on UI configuration — no more error-prone manual JSON editing.
**DONE** — Manual editing is much safer now — embedded `_readme` cheat-sheet, **Reload**, **Export/Import** with per-button merge + **Repair duplicate tabs**, encrypted secrets, and blank/gap spacers.
*Stage 1 done — per-button right-click menu:* **Edit button…** opens a dialog for every `ButtonDef` property (text/value, desc, hotkey, width, gap, left-align, show-text, tip-text, blank, sensitive), and **Delete button** (with confirm). Both mutate the live model, persist via `TabStore.SaveCurrent()`/`DeleteButton()` (which keep the crypto header + tombstones intact and re-stamp for merge), then rebuild the UI. Built on a back-link from `SymbolElement` → `ButtonDef`; code tabs (Emojis/Tools) get no menu.
*Add done — open-area right-click:* **Add button here…** (on the tab's empty area, menu hung off the ScrollViewer so the whole tab is reachable) opens the same dialog and inserts the new button next to where you clicked — after/before the nearest button by click position (`TabStore.InsertButton`), or as a first row in an empty tab (`TabStore.AddButton`).
*Blanks are first-class now:* blank spacer cells are placed/rendered as invisible, hit-testable cells that show a faint border on hover and carry the same Edit/Delete menu — so you turn a blank into a real button in place (uncheck "Blank" in the dialog) instead of inserting next to it. The dialog clears + greys-out the content fields while "Blank" is ticked, and `InsertButton` now *consumes* an adjacent blank rather than pushing the row wider (fixes the spurious wrap-row a positional insert used to create).
*Tab-level menu done:* right-click any tab header for **Add tab…**, **Edit tab…** (data tabs only), **Move left / Move right**, and **Delete tab** (with confirm; refuses the last remaining tab). **Edit/Add** open `TabEditDialog` — every data-tab attribute: name, columns, **button font size (pt)**, font family, button width/height, and the proportional / square / emoji-images / strip-emojis toggles (so the button `fontSize` is no longer JSON-only). New `TabStore.AddTab` / `DeleteTab` / `MoveTab` mutate the live `s_file.Tabs` and persist; the re-stamp handles it — a rename bumps the tab's clock (name is in `TabSig`), an add gets an id, a delete leaves a tombstone. Every `TabModel` now back-links its `TabEntry` (`Backing`) so a command finds its tab regardless of position. 5 tests added.
*Next stages:* live single-button regeneration and #2 (panels) for true WYSIWYG.

12. **DONE** — Make the buttons in a tab control originate at top left instead of center.
The button `Canvas` is now `HorizontalAlignment.Left` + `VerticalAlignment.Top`, so a tab narrower than the (locked) window width sits at the top-left edge-gap instead of being centred. (Done alongside #8/#9.)

13. **DONE** — A user-configurable **Favourites** section at the top of the Emojis tab (the catalog itself stays read-only). Right-click any catalog emoji → **Mark as favourite** (a copy is appended to the favourites); right-click a favourite → **Unfavourite**. Favourites can be **dragged to reorder** (within that tab only) — and because the emoji tab is a plain left-to-right flow, dropping re-registers the list in the new order and every row after the drop reflows (the end emoji of one row becomes the start of the next), with a blue insertion caret while dragging. The list persists in `%LocalAppData%\HenksHotkeys\favourites.json` (`FavouritesStore`), seeded on first run from the emoji that used to be hard-coded at the top of the tab, so nothing is lost. Each entry stores the emoji's **full codepoint sequence** as text (`"unicode": "U+2764 U+FE0F U+200D U+1F525"`, the authoritative value — explicit and hard to mangle) plus the glyph as a visual aid (`"char"`); the emoji sent/shown is decoded from the codepoints, and the friendly tooltip name is recovered from the catalog at render time (`EmojisTab.BackfillFavouriteNames`), so the file holds no free text. A favourites file from the first build (glyph + `desc`) is migrated in place on load (`FavouritesStore.Migrate` fills in the codepoints, order preserved, `desc` dropped) and rewritten cleanly. `EmojisTab.RegisterFavourites` reads the store and emits a "Favourites" heading; `SymbolElement.IsFavourite` tags them; the window special-cases `EmojisTab` buttons (`WireEmojiButton`) for the menu + reorder-drag (a plain click still sends the emoji). Section headings now use a **fixed font size** (`HeadingFontPt`, 12pt) independent of the tab's button font, so the heading isn't gigantic on the big-font Emojis tab (and is ready for per-category emoji headings later). Reorder index math + codepoint round-trip covered by 5 tests (no file touched).

14. **DONE** — Label sections in a tab.
Implemented as a *header row* (not a new container): a row with a `section` key is a header that labels the rows beneath it until the next header — `{"section": "My group"}` for a labelled divider, `{"section": ""}` for a plain line, optional `headerHeight` (px, default `Layout.SectionHeaderHeight` = 24). Backward compatible (existing flat `rows` files are unchanged; a tab with no header rows is one implicit unnamed section) and the merge needed no new nesting — header rows carry through `TabSig`/`NormalizeRows`/merge like blank rows, with their buttons cleared on load. Renders as a bold label on a separator line spanning the columns (`DataTabModel` emits `TabModel.SectionHeader`; `HotkeyWindow.BuildSectionHeader` draws it). Cheat-sheet updated.
*Bonus fix:* the merge row-rebuild used to drop the `blank` flag — it now preserves `blank`/`section`/`headerHeight` via `CloneRow`.
*Emoji category headings:* the Emojis tab (a cursor-flow code tab) now labels each category too — **Favourites, Smileys & Emotion, Hearts & Love, Gestures, People & Body, Animals, Plants & Sky, Food & Drink, Travel & Places, Activities, Objects, Symbols** — via a shared `TabModel.RegisterSectionHeader` helper that drops a full-width heading at the cursor and pushes the following buttons below it (replacing the old unlabelled half-row `CategoryGap`). Headings render at a fixed 12pt (`HeadingFontPt`), independent of the tab's big button font. Verified live (screenshot).

15. **DONE** — Drag a tab header to a new position; the new order is saved to tabs.json. A header press past the drag threshold captures that `TabItem` and shows a blue insertion caret (on the window overlay) at the nearest boundary between headers; dropping calls `TabStore.MoveTab`, which reorders `s_file.Tabs` and persists. Order is stored as the list order, and the merge keeps the local order, so a reorder survives sharing. The reorder is gated to header presses (a press inside the tab body is ignored) and coexists with the button-drag (which sets `Handled` on the tab control first). Menu **Move left / Move right** do the same without dragging. Reorder index math + merge-keeps-order covered by tests.

16. **DONE** — It would be very nice to be able to create and move buttons more than one row past the last row.

17. **DONE** — Hotkey editor now uses **Ctrl / Alt / Win / Shift toggle buttons** + a narrow key box instead of raw `^+!#` typing. The four toggles are equal-sized and the same height as the key box, stay pressed (accent) when selected (a styled `ToggleButton` template added to `DialogChrome`), and the box holds just the unmodified key (e.g. `F9`). `HotkeyParser.Split`/`Compose` convert between the toggles+key and the stored AHK string (composed in Ctrl/Alt/Win/Shift order); the key is validated on OK (single letter/digit or F1–F24, and a modifier without a key is rejected). 5 parser tests added (split, compose, round-trip).

18. **DONE** — Move buttons/headings to another tab by drag-and-drop, exactly as described. Ctrl+click to select (as in #25), then drag a selected item; **hovering its header for ~450 ms switches to that tab**, and dropping on the destination places the whole block there (anchored so the dragged item lands at the drop cell, relative layout preserved), collision-checked with **no inserting** — a conflict/out-of-bounds drop is refused and nothing changes. Same-tab drops still do the in-place block move.
*How the tricky bit is handled:* switching tabs mid-drag would normally kill the mouse capture (the source button leaves the visual tree), so a drag now **captures the persistent `TabControl`** rather than the dragged element; all move/dwell/drop logic runs off those captured events and `TabUnderCursor` hit-tests the headers. New primitive `TabStore.MoveSelectionToTab` (remove from source, reposition, add to destination; items keep their ids so merge sees a re-home, not delete+add). 2 tests added (cross-tab relocate keeps relative layout; refused on collision). Verified end-to-end: Ctrl-select A on ZZ → drag over ZY's header (switches) → drop → `ZZ=[B]`, `ZY=[X, A]`.
*Follow-ups (drag polish):* the whole button-drag was **unified** so a **single unselected button** drags to another tab too, not just a selection. Both a single button and a selection **capture the tab control up front on press** (the reliable pattern — transferring capture off the button *mid-move* doesn't take, which caused a single drag to lock up on drop and never switch tabs). Because the press captures the tab control (suppressing the button's own click), a data button's send is reproduced in `ClickNoDrag` when the press ends with no drag (double-click → Enter preserved; a light press-dim replaces the now-suppressed native pressed state). The ghost + drop cue no longer live on a tab's canvas (they vanished after a tab switch) but on a **window-level, click-through overlay** (`m_dragLayer`) that's always visible: a single drag shows one label ghost, a **block shows a sliding ghost for *every* selected item** (the static source overlays are hidden while in flight so nothing looks doubled), and the target cell lights up on whichever tab shows (cell geometry transformed from that canvas into overlay space). Single same-tab drags keep the insert caret/place semantics; block and cross-tab drops place at the pointed-at cell. Replaces the old per-element `Begin/Update/Drop/CancelButtonDrag` + `Begin/OnSel*Drag` paths with one `ArmDrag`/`OnDragMove`/`UpdateDrag`/`ShowDropCue`/`OnDragUp`/`CancelDrag` engine.

19. **DONE** — Allow sub-cell buttons: more than one button sharing a cell, split horizontally. Two fields on `ButtonDef` — `subcells` (how many share the cell, default 1, omitted from the file when 1) and `subcell` (0-based slot, left→right). `DataTabModel` sizes each to `(cellWidth − gaps)/subcells` and places it at its slot (full cell height). Guarding: the merge's collision resolver keys on `(row, col, subcell)`, so sub-cell siblings coexist while two buttons truly claiming the *same* slot are still separated; `ButtonSig` includes the slot; and `ShiftRight` now moves whole cell-groups (all buttons in a column together) so an insert-shift never splits a group. Edit dialog gained **Sub-cells** / **Sub-cell #** fields (validated 0 ≤ # < subcells). Old files default to 1 (no migration). Verified: 3-way and 2-way splits render at the right widths; 4 tests added.
*Follow-ups:* placing/dropping a sub-cell button into a cell that already holds one now **coexists** in a free slot instead of shifting the existing group aside — `Occupied` is conflict-aware (a whole-cell button fills the cell; mismatched divisions overlap; matching divisions clash only in the same slot) and a dragged sub-cell button targets the whole cell (no insert-shift). The edit dialog shows the **sub-cell # 1-based** while the stored value stays 0-based.

20. **DONE** — Popup-menu option to insert a heading. Right-click the open area → **Insert heading here…** opens a small dialog (label + start column + span) and drops the heading at the clicked cell. Built by extending `SectionDef` with `col` (start, default 0) and `span` (columns, default 0 = full width): a heading with `span > 0` is a partial-width label that is the **same height as a button row** (starts at its column, covers `span` cells), while the existing full-width `span 0` sections are unchanged (still `SectionHeaderHeight`). Headings carry a right-click **Edit heading… / Delete heading** menu (back-linked via `SectionHeader.Source`). Sections ride with the tab, so `TabSig`/`CloneAttrs` now include `col`/`span`, and `TabStore.AddHeading`/`DeleteHeading` persist. Verified: a col-1/span-3 button-row-height heading renders beside buttons and a classic full-width section; 2 tests added.

21. **DONE** — The open-area right-click menu now offers, below a separator: **Insert blank row** (opens an empty row at the click, pushing everything at/below down) and **Insert heading row…**. The old **Insert heading here…** (which places a heading in the clicked cell without moving anything) is renamed **Add heading here…**.
*Insert heading (refined):* nothing shifts until the heading dialog is saved. The heading starts at the clicked column and runs for its span; on save, **only the cells under the heading's columns** (at or below its row) shift down — content in other columns stays put. Because a multi-column entity (a wider heading, or a wide button) can't be split, `ShiftCellsDown` **staircases**: scanning downward it widens the affected column range at the row where such an entity overlaps the current range, and keeps it widened below (chaining). E.g. a 2–4 heading inserted above a 1–5 heading shifts cols 2–4 down from the top, then from the 1–5 heading's row the range widens to 1–5 (cols 1 & 5 shift from there down) — so nothing collides. A full-width divider below widens it to the whole row. **Insert blank row** stays a full-width row insert (`ShiftRowsDown`). 4 tests added (full-row shift, column-scoped shift, staircase-to-wider-heading, blank-row shift).

22. **DONE** — Heading text alignment (left/center/right, default **left**). New `SectionDef.align` field; the heading dialog picks it with a mutually-exclusive **Left/Center/Right** selector (three radio buttons styled as toggle buttons — `DialogControls.AlignPicker` + a `RadioButton` style in `DialogChrome`). `DataTabModel` carries it onto `SectionHeader.Align`; `BuildSectionHeader` sets the label's `HorizontalAlignment`. Tracked in `TabSig`/`CloneAttrs`. Verified: left/center/right headings render correctly.

23. **DONE** — Button text alignment (left/center/right, default **center**). Uses the existing `ButtonDef.align` (now supports "right"); the button dialog's old "Left-align" checkbox is replaced by the same **Left/Center/Right** `AlignPicker`. `BuildButton` maps it to `TextAlignment` (left/right get a small edge inset + ellipsis; center trims none). Already tracked via `ContentSig`. Verified: left/center/right buttons render correctly.

24. **DONE** — Numeric edit boxes get **▲/▼ spinners** (`DialogControls.Spin`): button dialog's Width / Sub-cells / Sub-cell #, and heading dialog's Start column / Span. Each nudges the whole number by ±1, clamped at its minimum.

25. **DONE** — Multi-selection of buttons *and* headings, then move/delete the whole set. **Ctrl+click** toggles any item into the selection (arbitrary, non-contiguous; buttons and headings together), shown with an accent overlay. **Dragging** a selected item moves the whole block by that cell-delta — collision-checked with **no inserting** (`TabStore.MoveSelection` refuses the move if any item would land out of bounds or on a cell held by an unselected item; the overlays slide as a live preview and snap back on a refused/collision drop). **Delete selected (N)** (and **Clear selection**) appear on every data-tab right-click menu when a selection exists (count refreshed on open); a plain click on the empty area clears it. Selection is per-tab and cleared on rebuild. `TabStore.MoveSelection`/`DeleteSelection` are written to extend to **cross-tab moves later** (#18). Verified end-to-end: Ctrl-selecting A & C (skipping B) then dragging moved both by +2 rows; 6 primitive tests added (delta move, collision refusal, selected-block overlap allowed, out-of-bounds, wide-button bounds, group delete).

26. **DONE (emoji tab)** — Collapsible section headers on the Emojis tab. Each heading shows a triangle to the left of the label — **▾ = open** (buttons visible), **▸ = collapsed** — and the whole header bar is click-to-toggle. Collapsing simply **doesn't build that section's buttons**, so (the tab being a top-down flow) everything below moves up automatically; expanding rebuilds them. State persists per section name in `%LocalAppData%\HenksHotkeys\emoji-collapsed.json` (`EmojiSectionStore`, re-read on Reload Configuration). Works for the Favourites section too. `TabModel.SectionHeader` gained `Collapsible`/`Collapsed`; `RegisterSectionHeader` takes them; `EmojisTab.Category`/`RegisterFavourites` skip the buttons when collapsed; `HotkeyWindow.BuildSectionHeader` draws the triangle and wires the toggle. Data-tab headers are unaffected (partial-width headings, `col`/`span`, deferred). Verified live: collapse "Smileys & Emotion" → triangle flips to ▸, smileys vanish, "Hearts & Love" flows up under it; click again restores.

27. **DONE** — Fleshed out the emoji categories + added a skin-tone selector. The big **People & Body** category had been commented out (a `/*;`…`*/;` block, ~2,300 lines) since the AHK days when it couldn't handle the newer emoji; it's re-enabled **base (yellow) only** — a script kept the 361 non-modifier entries and dropped the 1,763 per-emoji skin-tone permutations (which the global selector replaces). *We deliberately did not add all 4,009 images:* ~2,060 are skin-tone variants and ~1,638 ZWJ family/profession combos, so dumping them all would be a wall of near-duplicates — the base set + one global tone is the useful subset.
*Skin-tone selector:* six ✋ swatches pinned to the Emojis tab's top-right corner (active one outlined); picking one persists to `settings.json` (`SkinTone`) and rebuilds. Every toneable emoji is re-tinted — **both the image and the sent text** — by `EmojiSkin.Apply` (insert the modifier after the base rune, drop a redundant VS16), but only when Twemoji actually ships a toned image for it (`EmojiImageProvider.HasImage`), so families/couples and non-toneable emoji safely stay yellow. Favourites keep whatever the user stored. New: `EmojiSkin`, `SkinToneCommands`, `Settings.SkinTone`/`AppState.SkinTone`, `TabModel.SendAction` (so re-toned buttons rebuild their click). Verified live: People renders (hundreds of entries), and picking **Dark** re-tinted gestures + people (image + send) while food/travel and the blue family silhouettes stayed unchanged.
*Perf:* the Emojis tab has ~**1,196** buttons; profiling showed the cost of any rebuild is **WPF measuring/arranging those ~1,200 templated buttons (~2.2 s)** — construction and (cached) decode are minor. So *every* emoji-tab action that rebuilt the tab took ~3 s. **Skin tone is now applied in place** (`HotkeyWindow.RetintEmojiInPlace`): it swaps `Image.Source` + the sent char on the *existing* buttons (positions unchanged → no re-measure), so a tone change is ~**255 ms cold** (decoding that tone's images once) / ~**1–4 ms warm**. Enabled by a dynamic click action (reads `SymbolElement.Char` live) + `SymbolElement.BaseChar` (the un-toned identity). **Collapse/expand, favourite/unfavourite, and favourite-reorder** now use `HotkeyWindow.ReconcileEmojiTab`: it computes the desired layout with a throwaway `new EmojisTab()` (~2 ms), then **reconciles the existing buttons** by identity `(IsFavourite, BaseChar)` — reusing them (just repositioning; arrange is cheap), creating only the few that appeared, removing the few that went. Keeping the same model + button instances means all existing wiring (click / context menu / favourite drag) stays valid. Measured: **collapse ~84 ms**, unfavourite/reorder similar. **Expanding** a section still builds that section's buttons (create + first-time image decode), so it's ~1.5 s cold for a big never-shown section (less once its images are cached) — could be made instant too by caching hidden buttons instead of removing them. Verified live: collapse repositions everything below, unfavourite reflows the favourites, tone re-tints — all correct.

### Oddities
- **Answered** — `WireSymbolButton` vs `MouseDoubleClick`: a `Button` raises `Click` on *both* clicks of a double-click, so `MouseDoubleClick` would sit on top of two Clicks and send the text twice (text, Enter, text). A wait-and-see timer would instead delay the single-click send, which must be instant. So we send on the first Click and, if a second lands within `DoubleClickMs`, turn *that* one into the Enter. (Comment added.)
- **Answered** — `PtToDip` 4.0/3.0: a point is 1/72 inch, a WPF DIP is 1/96 inch, so points→DIPs scales by 96/72 = 4/3. Now written as named `DipsPerInch`/`PointsPerInch` constants.
- **Answered** — `ComputeFullSize` 330/320/64: the default (unsaved) height clamps the tallest tab's content into a [320, 330] DIP band, then adds 64 DIP of non-scrolling chrome (toolbar strip + tab-header row + top/bottom borders). Now named `DefaultViewportMin`/`DefaultViewportMax`/`VerticalChrome`.
- **DONE** — startup collapse flash: settings *are* loaded in the constructor (it already reads FavX/FavY), so the saved collapsed state is now applied there — before the first `Show()` — instead of flashing open at full size and snapping shut in `ShowUi`. The height-capture in `SetCollapsed` is guarded (`ActualHeight > 0`) so collapsing before the window is measured no longer zeroes the remembered expanded height. Verified: starts collapsed (101×35) and expands to a proper height.
- **DONE** — empty-cell hover skipped trailing columns: the hover was tracked on the `Canvas`, which is only as wide as the *content*, so cells in columns past the last button (e.g. the last column of a sparse tab) were outside it. The move is now tracked on the `ScrollViewer` (which fills the whole tab width), and the outline lives on the un-clipped canvas, so every column up to `columns` highlights. Verified: col 7 highlights on a tab whose content stops at col 1.
- **DONE** — a button overlapping a heading was un-draggable (and got trapped there): section headings are hit-testable (for their right-click menu) and were added *after* the buttons, so they painted on top and swallowed the button's mouse events. Headings now render *under* the buttons (added first), so an overlapping button stays on top — clickable and draggable back out. Verified: a button sharing a heading's cell drags away cleanly.