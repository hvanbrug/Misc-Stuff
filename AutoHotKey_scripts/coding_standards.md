- Allman braces on all blocks, including single statements.
- Two-space indent, no tabs.
- Align assignments vertically where practical.
- Prefer prefix over postfix operators.
- No space before opening paren; single space inside: if( x > 0 ), Func( a, b ).
- Match existing project style.
- Use descriptive variable and function names.
- Class member variables should have the 'm_' prefix.

Notes:
- Workspace copy of agent memory `/memories/coding_standards.md`.
- To apply, ask: "Replace in-memory coding standards with the workspace version."