<Window x:Class="NetworkShares.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NetworkShares"
        Title="Network Shares" Height="780" Width="600"
        MinHeight="520" MinWidth="500"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3">

  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="170"/>
    </Grid.RowDefinitions>

    <!-- All bar -->
    <Border Grid.Row="0" Background="#E8F0FE" BorderBrush="#C5D6F2" BorderThickness="1"
            CornerRadius="6" Padding="12" Margin="0,0,0,12">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" VerticalAlignment="Center">
          <TextBlock Text="All Mappings" FontSize="15" FontWeight="Bold"/>
          <TextBlock Text="Connect or disconnect every group at once." Foreground="#5B6B8C" FontSize="11"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Content="Connect All"    Command="{Binding ConnectAllCommand}"/>
          <Button Content="Disconnect All" Command="{Binding DisconnectAllCommand}" Margin="6,0,0,0"/>
          <Button Content="Refresh"        Command="{Binding RefreshCommand}"       Margin="6,0,0,0"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Groups -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Groups}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                    CornerRadius="6" Padding="12" Margin="0,0,0,10">
              <StackPanel>
                <!-- header -->
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Subtitle}" Foreground="#757575" FontSize="11"/>
                  </StackPanel>
                  <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Style="{StaticResource Pill}" Background="{Binding OverallBrush}" Margin="0,0,8,0">
                      <TextBlock Style="{StaticResource PillText}" Text="{Binding OverallText}"/>
                    </Border>
                    <PasswordBox Width="120" Margin="0,0,6,0" VerticalContentAlignment="Center"
                                 ToolTip="Password (optional — leave blank to use your saved / current credentials)"
                                 local:PasswordHelper.Attach="True"
                                 local:PasswordHelper.BoundPassword="{Binding Password, Mode=TwoWay}"/>
                    <Button Content="Connect"    Command="{Binding ConnectCommand}"/>
                    <Button Content="Disconnect" Command="{Binding DisconnectCommand}" Margin="6,0,0,0"/>
                  </StackPanel>
                </Grid>

                <!-- mappings -->
                <ItemsControl ItemsSource="{Binding Mappings}" Margin="0,10,0,0">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="34"/>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="108"/>
                          <ColumnDefinition Width="Auto"/>
                          <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Drive}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding Unc}" Foreground="#444444"
                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Border Grid.Column="2" Style="{StaticResource Pill}" Background="{Binding StatusBrush}" MaxWidth="108">
                          <TextBlock Style="{StaticResource PillText}" Text="{Binding StatusText}" TextTrimming="CharacterEllipsis"/>
                        </Border>
                        <Button Grid.Column="3" Style="{StaticResource MiniButton}" Content="Connect"
                                Command="{Binding ConnectCommand}" Margin="8,0,0,0"/>
                        <Button Grid.Column="4" Style="{StaticResource MiniButton}" Content="Disconnect"
                                Command="{Binding DisconnectCommand}" Margin="4,0,0,0"/>
                      </Grid>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <!-- log -->
    <TextBlock Grid.Row="2" Text="Activity" FontWeight="SemiBold" Margin="2,8,0,4"/>
    <TextBox Grid.Row="3" x:Name="LogBox"
             Text="{Binding Log, Mode=OneWay}" IsReadOnly="True"
             FontFamily="Consolas" FontSize="12"
             Background="#FAFAFA" BorderBrush="#E0E0E0"
             VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"
             HorizontalScrollBarVisibility="Auto"
             TextChanged="Log_TextChanged"/>
  </Grid>
</Window>
